# 北川弘喜 ポートフォリオ — 作業ルール

## このリポジトリは何か
ギタリスト／作編曲家 **北川弘喜** のポートフォリオサイト。1ページ完結の静的サイト。
GitHub Pages で公開する。閲覧者は **発注を検討している企業・制作会社の担当者**。ファンサイトではない。

## 作業を始める前に読むもの（この順で）
1. `docs/SPEC.md` — 実装仕様。作り始める前に必ず全部読む
2. `assets/css/kk-design-system.css` — デザインの判断基準
3. `docs/CONTENT.md` — 文言の正
4. `data/*.json` — 実績データの正

## 絶対に守ること

- **デザイントークンを増やさない。** 色・書体・字間・余白・イージングは
  `kk-design-system.css` の CSS 変数だけを使う。新しい色、角丸、影、グラデーションを足さない。
  足したくなったら、それは設計が間違っているサイン。SPEC.md に立ち戻る。
- **依存パッケージを入れない。** `npm install` しない。ビルドは Node 標準ライブラリのみで書く。
  CDN からライブラリを読み込むのも禁止（フォントの Google Fonts だけ例外）。
- **日本語のみ。** 英語版は作らない。欧文はセクションラベルと数字だけ。
- **実績データを HTML に直接書かない。** 必ず `data/*.json` に置き、`build.mjs` で `index.html` を生成する。
- **`index.html` を手で編集しない。** これは生成物。直すときはテンプレートかデータを直す。
- **事実を推測で埋めない。** 作詞か編曲か、年号、正式表記 — 不明な**事実**は絶対に捏造せず、
  年の列や担当の欄を空のまま出す。`docs/QUESTIONS.md` に追記して本人確認に回す。
- **ただし「本人の選択待ち」は仮で埋めてよい。** ヒーローのコピー、どの写真を使うか、
  といった**事実ではなく選択**の項目は、仮の値を入れて完成形まで組む。
  仮を使ったら必ず `docs/PROVISIONAL.md` に1行追記する。この2つを混同しないこと ——
  **捏造していいものは一つも無いが、決め打ちしていいものはある。**
- **`git push` しない。** GitHub Pages への公開は、本人確認が終わってから。
  それまではローカルで完成させ、コミットまでに留める。
- **写真は本人の資産。** 顔が切れるトリミングをしない。加工は縮小・WebP 変換・可読性のための暗幕まで。

## 進め方
- 1タスク = 1コミット。コミットメッセージは日本語で「何を・なぜ」。
- 変更したら `node build.mjs` を実行して `index.html` を再生成し、
  ブラウザで 1280px と 390px の両方を目で見てから完了とする。
- 迷ったら `docs/SPEC.md` の「受入基準」に照らす。そこに無い判断は勝手にせず、質問する。
